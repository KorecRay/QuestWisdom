var specialCharArray = [
    '!', '@', '#', '$', '%', '^', '&', '*', '(', ')', '_', '-',
    '+', '=', '[', ']', '{', '}', '|', ';', ':', "'", '"', ',',
    '.', '<', '>', '?', '/'
];

export class glitch {
    constructor() {
        this.enable = false;
        this.count = 0;
    }

    Text(id) {
        let text = document.getElementById(id);
        
    }
}